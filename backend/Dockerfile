FROM node:20-alpine

WORKDIR /app

# Install dependencies first for caching
COPY package*.json ./
RUN npm ci --only=production

# Copy source code
COPY . .

# Expose backend port
ENV PORT=5000
EXPOSE 5000

CMD ["npm", "run", "start"]
